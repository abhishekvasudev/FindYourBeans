import SwiftUI

struct HomeView: View {
    
    var body: some View {
        VStack(spacing: 0) {
            Text("TODO")
        }
        .withBackgroundColor()
    }
}

#Preview {
    HomeView()
}
